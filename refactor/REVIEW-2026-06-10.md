# Review run — 2026-06-10 (new findings only)

> Output of a [REVIEW.md](REVIEW.md) Phase 1–3 pass over `src/weakincentives/`
> (6 reviewer agents, one per package cluster), explicitly **de-duplicated
> against** [GOAL.md](GOAL.md), [M1](M1.md)–[M9](M9.md), and the H1–H4 hardening
> track in [BACKLOG.md](BACKLOG.md). Everything below is debt **not already
> covered** by the plan. Spot-checked claims are marked *(verified)*; findings
> that failed verification were dropped (e.g. "structured_output exports private
> helpers" — its `__all__` is clean; "`ReadOnlySliceAccessor` is dead" — it is
> the return type of `SessionView.__getitem__`).

## Disposition summary

- **Fold into existing milestones:** E1–E4 → [M7](M7.md) stage 1; P1 → [M5](M5.md)
  stage 5 (public testing/import surface); A6 → [M6](M6.md) stage 1 (ephemeral
  home consolidation should not preserve silent cleanup).
- **Candidate new hardening items:** H5 = R1+R2 (fail-closed session dispatch),
  H6 = A1–A5+A7 (adapter async & lifecycle hygiene), H7 = V1–V5 (evals
  simplification), H8 = F1–F4 (foundation tidy).

## State core — `runtime/`

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| R1 | **Reducer failures are logged-and-continued, not fail-closed** *(verified)*. A raising reducer is swallowed (`except Exception` → `logger.exception` → `continue`) after earlier reducers' ops already committed, leaving an event partially applied with no rollback and no caller-visible error — at odds with the fail-closed tenet and the transactional story | `runtime/session/session_dispatch.py:178-188` | Decide a contract: collect reducer errors and raise a typed `DispatchError`, or record failures into a dead-letter slice; never silently half-apply | M | high |
| R2 | **Silent `append_all` ledger fallback for unregistered event types** *(verified)*. Dispatching a type with no registered reducer implicitly creates a slice with ledger semantics; a typo'd event type "works" forever | `runtime/session/session_dispatch.py:132-139` | Make ledger semantics explicit (register-as-ledger API) or document on `SessionProtocol.dispatch` and log at info, not debug | S | high |
| R3 | **Snapshot/restore fragmented across three modules** — `Session.restore`/`snapshot` delegate to `SessionSnapshotter`, while `session_cloning.py` re-implements overlapping snapshot helpers with separate locking paths | `runtime/session/session.py:367-395,484-510`; `session_snapshotter.py:161-200`; `session_cloning.py:35-100` | Consolidate all snapshot/restore/clone logic into `SessionSnapshotter`; one locking discipline | M | med |
| R4 | **`ControlDispatcher` / `TelemetryDispatcher` are aliases of one protocol** — the distinction is documentation-only; nothing prevents cross-use | `runtime/events/__init__.py:104-107` | Either distinct protocols or one `Dispatcher` with clear naming | S | med |

## Definition core — `prompt/`

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| P1 | **`prompt/__init__.py` exports ~95 symbols in one flat namespace**, mixing the core surface (`PromptTemplate`, `Section`, `Tool`) with overrides subclasses, validators, and normalization helpers; the de-facto public API is unbounded | `prompt/__init__.py:346-426` | Curate a tiered surface (core / extension / internal) as part of M5's "definitions are the paved road" work | M | high |
| P2 | **`_validators.py` mixes five validation concerns in 527 LOC** (section params, tools, registry invariants, task examples, param nodes) and reaches into registry privates | `prompt/_validators.py` (e.g. `:174-175,196,214`) | Split by concern; give the registry an internal validation API instead of private-attribute access | M | med |
| P3 | **`FeedbackContext` re-filters session slices per query** — `last_feedback_for_provider` et al. each rescan; `tool_calls_since_last_feedback_for_provider` filters the same slice twice per call | `prompt/feedback.py:200-301` | Compute per-provider indexes once per context (contexts are per-evaluation, so a plain cached property suffices) | S | med |
| P4 | **Render-error enrichment loses nested context** — `PromptRenderError` is enriched only when fields are absent, so 3-deep section failures report the innermost path only | `prompt/rendering.py:486-497` | Chain/stack section paths on re-raise | S | med |

## Contract core — `adapters/`

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| A1 | **Re-export-only modules in `claude_agent_sdk`** *(verified)* — `_async_utils.py`, `_visibility_signal.py`, and `_bridge.py` exist solely to re-export `_shared` symbols | `adapters/claude_agent_sdk/_async_utils.py`, `_visibility_signal.py`, `_bridge.py` | Delete; import from `_shared` directly | S | high |
| A2 | **ACP chunk-text extraction duplicated with divergent semantics** — two implementations of the same content-block unpacking; only one handles `list[TextContentBlock]` | `adapters/acp/_prompt_loop.py:46-83` vs `acp/_transcript.py:47-56` | One ACP content-extractor helper | S | high |
| A3 | **Content-block unpacking tripled inside `claude_agent_sdk`** — transcript collector, message extraction, and hook tools each unpack text/list/fallback shapes with different edge handling | `adapters/claude_agent_sdk/_transcript_collector.py:54-76`; `_message_extraction.py`; `_hook_tools.py` | One content parser for the package | S | med |
| A4 | **`MCPHttpServer` hand-rolls thread + private event loop + flag/join shutdown** — manual `new_event_loop` in a spawned thread, uvicorn server, timeout-based join; leak/deadlock-prone | `adapters/acp/_mcp_http.py:143-156,221-308` | Single async lifecycle (or executor-based) with deterministic shutdown | M | med |
| A5 | **Event loop churned per call** — `run_async` creates and closes a fresh loop on every `evaluate()`; `_mcp_http` replicates the pattern | `adapters/_shared/_async_utils.py:25` | One owned async runtime per adapter instance (natural companion to M6's shared turn loop) | M | med |
| A6 | **Silent cleanup in `finally` paths** — `shutil.rmtree(ignore_errors=True)` / ephemeral-home cleanup with no logging; cleanup failures (and thus environment corruption) are invisible | `adapters/claude_agent_sdk/adapter.py:475-476`; `opencode_acp/_ephemeral_home.py` | Log failures; fold the policy into M6's single ephemeral-home base | S | med |
| A7 | **Hook state sprawls across three modules** — `HookContext` is mutated from `_hook_context.py`, `_hook_tools.py` (~80 private helpers), and `_hooks.py` factories with no ownership boundary | `adapters/claude_agent_sdk/_hook_context.py`; `_hook_tools.py`; `_hooks.py` | Consolidate helpers into `HookContext` methods; typed tool-data parsing | M | med |

## Evidence core — `cli/`, `debug/` (fold into [M7](M7.md) stage 1)

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| E1 | **Stringly-typed filter plumbing** — `get_logs`/`get_transcript` take 6+ optional params, assemble magic-key dicts, and feed twin `_build_log_filters`/`_build_transcript_filters` (~80 LOC structural duplication) | `cli/_bundle_store.py:131-155,175-214,368-412` | One frozen `Filter` dataclass + one builder, designed into M7's table-spec descriptor | S–M | high |
| E2 | **Bundle-path resolution duplicated** — dir-vs-file normalization implemented in both `BundleStore` and `query.py` | `cli/_bundle_store.py:246-255,704-715` vs `cli/query.py:273` | One `resolve_bundle_path` home | S | med |
| E3 | **`_read_spec` ≡ `_read_guide`** — same case-insensitive lookup + header injection twice | `cli/_docs.py:125-144` vs `:146-164` | One `_read_doc(dir, name)` helper | S | high |
| E4 | **One-caller schema envelope dataclasses** — `ColumnInfo`/`TableInfo`/`SchemaOutput` exist only for `get_schema` | `cli/query.py:52-94,254-260` | Inline or keep only if the M7 descriptor reuses them | S | med |

## `contrib/`, `evals/`, `skills/`

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| V1 | **Ten near-identical evaluator closures** — every session evaluator is the same fetch-filter-score wrapper written out by hand; `all_of`/`any_of` differ only in the aggregator (`mean` vs `max`) | `evals/_session_evaluators.py:44-251`; `evals/_evaluators.py:184-273` | Parameterized evaluator/combinator factories (~260 → ~80 LOC) | M | high |
| V2 | **`EvalLoop` control flow fragmented over 7 one-caller privates** with bundle/non-bundle near-duplication (`_evaluate_sample_with_bundle` vs `…_without_bundle`; `_compute_score` returning different shapes per path) | `evals/_loop.py:149-500` (esp. `:216-360,362-383`) | Collapse to ~3 methods with one scoring path | M | high |
| V3 | **Hand-rolled annotation introspection** — `is_session_aware` + 2 helpers (~50 LOC) re-implement `typing.get_type_hints` semantics incl. string-annotation resolution | `evals/_evaluators.py:72-164` | Use `get_type_hints` with a small fallback | S | high |
| V4 | **`_coerce` re-implements `serde.parse` dispatch** for one call site | `evals/_types.py:93-117,177-180` | Delete; call `serde.parse` | S | med |
| V5 | **`_helpers.py` is trivial forwarders** — `submit_dataset`/`submit_experiments`/`collect_results` are 5–10-line mailbox loops with docstrings longer than the code | `evals/_helpers.py:25-131` | Inline into `EvalLoop` or demote to documented examples | S | med |

## Foundation — `formal/`, `types/`, `experiment.py`, `resources/`, `dbc/`

| ID | Finding | Evidence | Proposed change | Size | Conf |
|----|---------|----------|-----------------|------|------|
| F1 | **`formal/` sits in the foundation layer but has exactly one consumer** *(verified)* — only `contrib/mailbox/_redis_spec.py` and `_redis.py` import it; ~1.0k LOC + TLA+ tooling in core for a peripheral feature | `grep` over `src/`: importers are `contrib/mailbox/{_redis_spec,_redis}.py` only | Move to `contrib/formal/` (or an optional extra); foundation stays substrate-only | M | med–high |
| F2 | **Dead type exports** *(verified)* — `ParseableDataclassT`, `JSONArrayT`, `JSONObjectT` have zero importers outside `types/` (`SupportsDataclassOrNone` **is** used; keep it) | `types/__init__.py` | Delete the three unused aliases | S | high |
| F3 | **`Experiment` ships two identical sentinels** — `BASELINE` and `CONTROL` are behaviorally 1:1 | `experiment.py` | Keep `BASELINE`; document it as the control | S | med |
| F4 | **`ResourceRegistry` has two construction APIs** — `.of(Binding, …)` vs `.build(mapping)` do the same job with different shapes | `resources/registry.py:71-150` | Pick one entrypoint; delete the other | S–M | med |
| F5 | **`dbc/__init__.py` is a 545-LOC single module** mixing predicate evaluation, decorator wrapping, invariant metaprogramming, and `ContextVar` state — cohesive enough to defer, but the next dbc feature should trigger the split | `dbc/__init__.py` | Split into `_predicates` / `_decorators` / `_invariants` when next touched | M | low–med |

## Notes on near-misses (reviewed, not filed)

- `clock.py`'s five narrow protocols were flagged as over-granular, but
  CLAUDE.md explicitly prescribes depending on narrow clock protocols — working
  as designed.
- Optional `clock`/`sleeper` parameters on adapter functions were flagged as
  signature noise, but injected clocks are repo law; not debt.
- `TranscriptCollector` (639 LOC, one caller) is large but cohesive and
  adapter-specific; revisit only if a second consumer appears.
